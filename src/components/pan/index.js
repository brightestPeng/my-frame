
import { createStore } from "redux"; 
import { } from "react-redux"; 
import { } from "react-router"; 

export default pan = ()=>{

	const models = ()=>{
		
	}

	const app = createStore();

	return { router,models,start }
}